describe("API", () => {
  context("GET", () => {
    it("returns a list of all of the", () => {});
  });

  context("POST", () => {
    it("creates a new", () => {});
  });

  context("PATCH", () => {
    it("updates a", () => {});
  });

  context("DELETE", () => {
    it("deletes a", () => {});
  });
});
